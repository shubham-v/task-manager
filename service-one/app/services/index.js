const ErrorService = require('./ErrorService')
const LogService = require('./LogService')
const MongoService = require('./MongoService')

module.exports = {
    ErrorService, LogService, MongoService
}